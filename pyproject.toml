[build-system]
requires = ["setuptools>=61"']
build-backend = "setuptools.build_meta"

[project]
name = "undertone"
description = "Theme compiler in Python"
version = "0.0.1"

[project.optional-dependencies]

[tool.ruff]

[tool.pytest.ini_options]
